import UIKit

let maximumNumberOfLoginAttempts = 3 // Sintaxis para declarar constantes en swift

var currentLoginAttempts = 0 // Sintaxis para declarar variables en swift

var x = 0.0, y = 0.0, z = 0.0 // Se pueden declarar variables en una sola linea

var welcomeMessage : String

welcomeMessage = "Hola, que tal?"

var red, green, blue : Double

let π = 3.141592 // option + p sirve para escribir el número pi
let 👽 = "Alien" // Se pueden usar emojis para definir variables o constantes

print(👽) // Alien debe ser su salida

print("El número de login actual es: \(currentLoginAttempts) de un total disponible de \(maximumNumberOfLoginAttempts)")

// 
